apiVersion: 1

providers:
  - name: ENMeta
    orgId: 1
    folder: ENMeta
    type: file
    disableDeletion: false
    editable: true
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
